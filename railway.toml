[deploy]
startCommand = "alembic stamp saas_multitenant_usage --purge 2>/dev/null || true && alembic stamp add_role_status_logs 2>/dev/null || true && alembic upgrade heads && python bot.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
