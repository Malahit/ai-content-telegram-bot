# RAG (Retrieval-Augmented Generation) dependencies
# Install these only if you need RAG features to enhance content generation
# with your own knowledge base documents.
#
# Installation: pip install -r requirements-rag.txt
#
# These dependencies enable:
# - Document loading and parsing (pypdf, unstructured)
# - Vector embeddings and similarity search (sentence-transformers, faiss-cpu)
# - LangChain framework integration (langchain, langchain-community, langchain-openai)
# - Auto-reloading on file changes (watchdog)

langchain==0.3.2
langchain-community==0.3.1
langchain-openai==0.2.2
faiss-cpu==1.9.0.post1
pypdf==5.1.0
unstructured==0.18.24
watchdog==3.0.0
sentence-transformers>=2.2.2
